- if current_user
  %div{id: "like_form_#{entity.class}-#{entity.id}"}
    = render "shared/dislike", entity: entity
    %strong{id: "likes_#{entity.class}-#{entity.id}"}
      = likes_count(entity)
    = render "shared/like", entity: entity
